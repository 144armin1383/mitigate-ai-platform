"""Optional external integrations for MITIGATE AI.

Integrations in this package must never become mandatory dependencies of the
MITIGATE AI core runtime. Every integration must have a native-safe fallback.
"""
