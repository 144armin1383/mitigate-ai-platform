# Secrets remain external to Git.
# Protected runtime values are supplied externally.
# Real access values must never be committed.
# This committed template contains placeholders only.

# Generic provider configuration (portable)
PROVIDER="<PROVIDER>"
PROVIDER_API_KEY="<PLACEHOLDER>"
PROVIDER_BASE_URL="<PROVIDER_BASE_URL>"
PROVIDER_MODEL="<PROVIDER_MODEL>"

# MITIGATE AI compatibility variables (mirror of generic provider config)
MITIGATE_AI_PROVIDER="<PROVIDER>"
MITIGATE_AI_PROVIDER_API_KEY="<PLACEHOLDER>"
MITIGATE_AI_PROVIDER_BASE_URL="<PROVIDER_BASE_URL>"
MITIGATE_AI_PROVIDER_MODEL="<PROVIDER_MODEL>"
